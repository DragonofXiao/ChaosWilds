# Godot 4.x GDScript 开发规范

## 代码风格
- 使用 GDScript 2.0 语法
- 所有变量、函数参数、返回值必须显式声明类型（强制静态类型）
  - 正确: var health: int = 100
  - 错误: var health = 100
- 遵循 Godot 官方风格指南
- 不要使用已弃用的 API

## 文件与命名
- 场景文件：.tscn 格式,snake_case 命名(如 player_stats.tscn)
- 脚本文件：.gd 格式
- 信号命名：名词_动词过去式(如 health_changed)
  - 注意：Godot 内置信号（如 area_entered）保持原样，不强行统一

## 导出与节点
- 优先使用 @export 导出变量，方便在编辑器中调整
- 所有 UI 节点（按钮、面板、进度条等）必须作为 CanvasLayer 的子节点

## 修改边界（防止 AI 过度重构）
- 修改代码时，仅改动与需求直接相关的部分
- 新增功能优先考虑新建脚本/场景，避免修改现有核心文件
- 如需修改核心架构，必须先提问确认
- 每次修改后，确保场景文件（.tscn）的 uid 没有重复或冲突